{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**"]
        },
        "build:production": {
            "dependsOn": ["^build"],
            "outputs": ["build/**"]
        },
        "build:staging": {
            "dependsOn": ["^build"],
            "outputs": ["build/**"]
        },
        "build:library": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**"]
        },
        "build:storybook": {
            "dependsOn": ["^build"],
            "outputs": ["storybook-static/**"]
        },
        "test": {
            "inputs": [
                "src/**/*.{ts,tsx}",
                "tests/**/*.{ts,tsx}",
                "src/**/*.test.{ts,tsx}",
                "src/**/*.sol",
                "tests/**/*.sol",
                "babel.config.json"
            ]
        },
        "start": {
            "dependsOn": ["^build"]
        },
        "publish": {
            "dependsOn": ["^build"]
        },
        "size-limit": {
            "dependsOn": ["build"]
        },
        "lint": {},
        "format": {}
    },
    "globalEnv": [
        "NODE_ENV",
        "FATHOM_SITE_ID",
        "FATHOM_API_KEY",
        "REACT_APP_INFURA_PROJECT_ID",
        "REACT_APP_WALLETCONNECT_PROJECT_ID",
        "REACT_APP_FATHOM_SITE_ID",
        "CI",
        "STAGING"
    ]
}
